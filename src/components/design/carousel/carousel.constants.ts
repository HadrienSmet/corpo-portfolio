export const OPACITY = {
    max: 0,
    min: 1,
} as const;
export const SCALE = {
    max: 1.2,
    min: 0.5,
} as const;
/** Refers to a percentage */
export const TRANSLATE_Y = {
    max: 8,
    min: -2,
} as const;
